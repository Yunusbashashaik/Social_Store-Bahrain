{
  "name": "social-store-bahrain",
  "private": true,
  "version": "1.0.0",
  "description": "Social Store — digital subscription marketplace (Bahrain / BHD)",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "node app.js",
    "lint": "npm run lint -w client && npm run lint -w server",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
