/**
 * Hardcoded catalog for the storefront.
 * Put each service here (English/Arabic names and descriptions, prices, imageFile).
 * Put the JPEG in client/public/images/, images/, and docs/images/.
 *
 * An empty list does not delete live database rows.
 * After this array has items, every Node start restores exactly this list.
 * Editing this file and redeploying updates the site; the catalog does not vanish.
 */
export const DEFAULT_SERVICES = [];
